#include "../include/so_long.h"


/*
static int up_down(t_vars *vars, int command)
{
    (void) command;
    (void) vars;
    return (0);
}
static int lift_right(t_vars *vars, int command)
{
    (void) command;
    (void) vars;
    return (0);
}

static int placing_graphics(t_vars *vars)
{
    (void) vars;
    return (0);
}
*/

// int	controls_working(int command, t_vars *vars)
// {
// 	int	move;

// 	move = 0;
// 	if (command == 53)
// 		exit_point(vars);
// 	if (command == 125 || command == 126)
// 		move = up_down(vars, command);
// 	else if (command == 123 || command == 124)
// 		move = lift_right(vars, command);
// 	if (move)
// 	{
// 		placing_graphics(vars);
// 	}
// 	return (1);
// }